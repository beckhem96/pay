spring:
  application:
      name: membership-service
  server:
    port: 8080
  # H2 ??
  h2:
    console:
      enabled: true
      path: /h2-console

  # Datasource ??
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
    username: user
    password: "" # YAML??? ? ?? ??? ?? ?? "" ???? ?? ?????.

  # JPA ?? (?? ?? ??!)
  jpa:
    hibernate:
      ddl-auto: create-drop

  # Swagger(Springfox) compatibility for Spring Boot 2.6+
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher